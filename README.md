# Inkorium

Base limpia de React + TypeScript + Vite conectada a Supabase.

```bash
npm install
npm run dev
```

La aplicación usa `VITE_SUPABASE_URL` y `VITE_SUPABASE_PUBLISHABLE_KEY`. Nunca requiere una clave `service_role` en el navegador.
