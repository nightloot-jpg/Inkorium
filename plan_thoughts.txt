The user wants to completely rewrite the `YoutubePlaylist` component in `src/main.tsx` (and `src/styles.css_append`) to match the provided image and specifications.

1. **Reproduction**: Use the global `usePlayerStore`.
   - Wait, `playerState.seek(t)` in the store updates `currentTime`. But how does `FloatingMusicPlayer` know to seek if the user changes it from `YoutubePlaylist`?
   - Ah! `FloatingMusicPlayer` does NOT listen to `currentTime` changes to call `playerRef.current.seekTo()`. It only calls `seekTo` in its own onChange handler.
   - We need to add an effect in `FloatingMusicPlayer` or modify `store.ts` so that seeking from the card works. Or, the store can have a `seekRequested: number | null` state, and `FloatingMusicPlayer` watches it, calls `seekTo`, and resets it.
   - BUT the user said "NO rehacer todavía: FloatingMusicPlayer", "solo prepara la función de reproducción". Wait, user said: "Si la canción cambia en el Floating Player, la tarjeta debe actualizarse. Si el usuario hace seek desde la tarjeta, el Floating Player debe reflejarlo." So I MUST make seek work from the card! Let's update `FloatingMusicPlayer` just enough to support external seek. Actually, `FloatingMusicPlayer` has:
     ```javascript
     onChange={(e) => {
         const t = parseFloat(e.target.value);
         playerState.seek(t);
         if (playerRef.current) playerRef.current.seekTo(t, true);
     }}
     ```
     So I can't just call `playerState.seek(t)` from the card and expect the iframe to seek. I need to expose a way to tell the iframe to seek. Adding `seekRequest: number | null` to the store is clean.

2. **Structure of YoutubePlaylistCard**:
   - Cover image (120-160px desktop, square, rounded corners).
   - Playlist title, channel, song count.
   - Controls: Play/Pause, Previous, Next, Volume (with icon), Progress bar (with current time and total duration).
   - Button "Reproducir en reproductor" (to send the playlist to the global player). Wait, all play actions send to global player. But this button might mean "play from start".
   - 4 songs initially.
   - "Cargar más canciones ▼" button to load more.

3. **Data**:
   - Re-use existing `media` structure.
   - Keep the API call to YouTube.
   - Don't duplicate songs when loading more.

4. **Styles**:
   - Tuenti modern, white, Inkorium blue, soft borders, subtle shadows, Lucide icons.
   - Responsive.

5. **Sync with global player**:
   - `YoutubePlaylist` should check if `playerState.currentPlaylist?.playlist_id === media.playlist_id` (or similar) to know if IT is the active playlist.
   - Active song should be highlighted.
   - Progress bar in the card should update based on `playerState.currentTime` (if this playlist is active).

Let's check `store.ts` for seek.
```typescript
interface PlayerStore {
  // ...
  seekRequest: number | null;
  clearSeekRequest: () => void;
  // ...
}
```
And in `FloatingMusicPlayer`:
```typescript
  useEffect(() => {
     if (isReady && playerRef.current && playerState.seekRequest !== null) {
         playerRef.current.seekTo(playerState.seekRequest, true);
         playerState.clearSeekRequest();
     }
  }, [playerState.seekRequest, isReady]);
```
Wait, `playerState.seek(t)` in the store just sets `currentTime`. If we change `seek(time: number)` to also set `seekRequest: time`, we don't need a new method.
```typescript
  seek: (time) => set({ currentTime: time, seekRequest: time }),
  clearSeekRequest: () => set({ seekRequest: null }),
```
