import React, { useCallback, useEffect, useMemo, useState } from 'react';
import { ArrowLeft, Inbox, Mail, Reply, Send, SendHorizontal, Trash2 } from 'lucide-react';
import { useInkorium } from '../context/InkoriumContext';
import { supabase } from '../lib/supabase';
import { PrivateMessage } from '../types';
type DbMessage = { id:string; sender_id:string; recipient_id:string; subject:string; body:string; is_read:boolean; created_at:string };
export const MessagesViewV2:React.FC=()=>{const{currentUser,users,viewUserProfile}=useInkorium();const[messages,setMessages]=useState<PrivateMessage[]>([]);const[mode,setMode]=useState<'recibidos'|'enviados'|'enviar'>('recibidos');const[selectedMessage,setSelectedMessage]=useState<PrivateMessage|null>(null);const[targetUserId,setTargetUserId]=useState('');const[subject,setSubject]=useState('');const[body,setBody]=useState('');const[loading,setLoading]=useState(true);const[sending,setSending]=useState(false);const[error,setError]=useState('');const userMap=useMemo(()=>new Map(users.map(u=>[u.id,u])),[users]);const mapMessage=useCallback((r:DbMessage):PrivateMessage=>{const s=userMap.get(r.sender_id),t=userMap.get(r.recipient_id);return{id:r.id,emisorId:r.sender_id,emisorNombre:s?`${s.nombre} ${s.apellidos}`.trim():'Usuario',emisorAvatar:s?.avatar||'',receptorId:r.recipient_id,receptorNombre:t?`${t.nombre} ${t.apellidos}`.trim():'Usuario',asunto:r.subject||'Sin asunto',mensaje:r.body,fecha:new Date(r.created_at).toLocaleString('es-ES'),leido:r.is_read}},[userMap]);const loadMessages=useCallback(async()=>{if(!supabase||!currentUser.id)return;setLoading(true);setError('');try{const{data,error:e}=await (supabase.from('private_messages') as any).select('id,sender_id,recipient_id,subject,body,is_read,created_at').order('created_at',{ascending:false});if(e)throw e;setMessages(((data as DbMessage[])||[]).map(mapMessage))}catch(e){console.error('Private messages load failed:',e);setError(e instanceof Error?e.message:'No se pudieron cargar los mensajes.')}finally{setLoading(false)}},[currentUser.id,mapMessage]);useEffect(()=>{void loadMessages()},[loadMessages]);useEffect(()=>{if(!supabase||!currentUser.id)return;const ch=supabase.channel(`private-messages-${currentUser.id}`).on('postgres_changes',{event:'*',schema:'public',table:'private_messages',filter:`recipient_id=eq.${currentUser.id}`},()=>void loadMessages()).on('postgres_changes',{event:'*',schema:'public',table:'private_messages',filter:`sender_id=eq.${currentUser.id}`},()=>void loadMessages()).subscribe();return()=>{void supabase.removeChannel(ch)}},[currentUser.id,loadMessages]);const received=messages.filter(m=>m.receptorId===currentUser.id),sent=messages.filter(m=>m.emisorId===currentUser.id),others=users.filter(u=>u.id!==currentUser.id);const open=async(m:PrivateMessage)=>{setSelectedMessage(m);if(m.leido||m.receptorId!==currentUser.id||!supabase)return;const{error:e}=await (supabase.from('private_messages') as any).update({is_read:true}).eq('id',m.id).eq('recipient_id',currentUser.id);if(!e){setMessages(p=>p.map(x=>x.id===m.id?{...x,leido:true}:x));setSelectedMessage(p=>p?{...p,leido:true}:p)}};const send=async(e:React.FormEvent)=>{e.preventDefault();const b=body.trim();if(!supabase||!currentUser.id||!targetUserId||!b||sending)return;setSending(true);setError('');try{const{data,error:e}=await (supabase.from('private_messages') as any).insert({sender_id:currentUser.id,recipient_id:targetUserId,subject:subject.trim().slice(0,200)||'Sin asunto',body:b}).select('id,sender_id,recipient_id,subject,body,is_read,created_at').single();if(e)throw e;if(data)setMessages(p=>[mapMessage(data as DbMessage),...p.filter(x=>x.id!==(data as any).id)]);setTargetUserId('');setSubject('');setBody('');setMode('enviados')}catch(e){console.error('Private message send failed:',e);setError(e instanceof Error?e.message:'No se pudo enviar el mensaje.')}finally{setSending(false)}};const del=async(m:PrivateMessage)=>{if(!supabase||!confirm('¿Borrar este mensaje?'))return;const{error:e}=await (supabase.from('private_messages') as any).delete().eq('id',m.id);if(e){setError(e.message);return}setMessages(p=>p.filter(x=>x.id!==m.id));setSelectedMessage(null)};return <div className="w-full max-w-[1720px] 2xl:max-w-[1850px] mx-auto px-3 sm:px-6 lg:px-8 py-4"><div className="grid grid-cols-1 md:grid-cols-12 gap-4"><div className="md:col-span-4 lg:col-span-3 space-y-3"><div className="bg-white rounded border border-[#ccd5df] overflow-hidden text-xs shadow-xs"><div className="bg-[#f0f4f8] px-3 py-2 border-b border-[#ccd5df] font-bold text-gray-700 flex items-center gap-1.5"><Mail className="w-4 h-4 text-[#3869A0]"/>Mensajería Privada</div><div className="divide-y divide-gray-100 font-medium"><button onClick={()=>{setMode('enviar');setSelectedMessage(null)}} className={`w-full text-left px-3 py-2.5 flex items-center gap-2 cursor-pointer ${mode==='enviar'?'bg-[#3869A0] text-white font-bold':'hover:bg-blue-50 text-gray-800'}`}><SendHorizontal className="w-3.5 h-3.5"/>Enviar mensaje</button><button onClick={()=>{setMode('recibidos');setSelectedMessage(null)}} className={`w-full text-left px-3 py-2.5 flex items-center justify-between cursor-pointer ${mode==='recibidos'&&!selectedMessage?'bg-[#3869A0] text-white font-bold':'hover:bg-blue-50 text-gray-800'}`}><span className="flex items-center gap-2"><Inbox className="w-3.5 h-3.5"/>Mensajes Recibidos</span><span className="bg-gray-100 text-gray-600 text-[10px] px-1.5 rounded-full">{received.filter(m=>!m.leido).length}</span></button><button onClick={()=>{setMode('enviados');setSelectedMessage(null)}} className={`w-full text-left px-3 py-2.5 flex items-center justify-between cursor-pointer ${mode==='enviados'&&!selectedMessage?'bg-[#3869A0] text-white font-bold':'hover:bg-blue-50 text-gray-800'}`}><span className="flex items-center gap-2"><Send className="w-3.5 h-3.5"/>Mensajes Enviados</span><span className="bg-gray-100 text-gray-600 text-[10px] px-1.5 rounded-full">{sent.length}</span></button></div></div><div className="bg-blue-50/70 border border-blue-200 rounded p-3 text-[11px] text-gray-600"><p className="font-bold text-[#3869A0]">💡 Mensajes privados seguros</p><p>Los mensajes privados de Inkorium solo los podéis leer tú y tu destinatario.</p></div></div><div className="md:col-span-8 lg:col-span-9"><div className="bg-white rounded border border-[#ccd5df] p-4 shadow-xs min-h-[400px]">{error&&<div className="mb-3 rounded border border-red-200 bg-red-50 px-3 py-2 text-xs text-red-700">No se pudo completar la operación: {error}</div>}{loading?<div className="text-center py-16 text-gray-400 text-xs">Cargando mensajes...</div>:selectedMessage?<div className="space-y-4 text-xs"><div className="flex items-center justify-between pb-3 border-b border-gray-200"><button onClick={()=>setSelectedMessage(null)} className="flex items-center gap-1.5 text-[#3869A0] font-bold hover:underline"><ArrowLeft className="w-4 h-4"/>Volver a la lista</button><div className="flex gap-2">{selectedMessage.receptorId===currentUser.id&&<button onClick={()=>{setTargetUserId(selectedMessage.emisorId);setSubject(selectedMessage.asunto.startsWith('Re: ')?selectedMessage.asunto:`Re: ${selectedMessage.asunto}`);setBody('');setSelectedMessage(null);setMode('enviar')}} className="px-3 py-1 bg-[#3869A0] text-white rounded font-bold flex items-center gap-1"><Reply className="w-3 h-3"/>Responder</button>}<button onClick={()=>void del(selectedMessage)} className="p-1 text-gray-400 hover:text-red-600"><Trash2 className="w-4 h-4"/></button></div></div><div className="flex items-start gap-3 p-3 bg-gray-50 rounded border border-gray-200"><img src={selectedMessage.emisorAvatar} alt="" className="w-10 h-10 rounded object-cover border border-gray-300" onClick={()=>viewUserProfile(selectedMessage.emisorId)}/><div className="flex-1"><h2 className="text-sm font-bold text-gray-900">{selectedMessage.asunto}</h2><p className="text-gray-600 mt-0.5">De: <span onClick={()=>viewUserProfile(selectedMessage.emisorId)} className="text-[#3869A0] font-bold hover:underline cursor-pointer">{selectedMessage.emisorNombre}</span> para <span className="font-semibold">{selectedMessage.receptorNombre}</span></p><span className="text-[10px] text-gray-400 block mt-0.5">{selectedMessage.fecha}</span></div></div><div className="p-4 bg-white rounded border border-gray-200 text-gray-800 whitespace-pre-line leading-relaxed text-xs min-h-[160px]">{selectedMessage.mensaje}</div></div>:mode==='enviar'?<div className="space-y-4 text-xs"><h2 className="font-bold text-sm text-gray-900 pb-2 border-b border-gray-200 flex items-center gap-1.5"><SendHorizontal className="w-4 h-4 text-[#3869A0]"/>Redactar nuevo mensaje privado</h2><form onSubmit={send} className="space-y-3"><div><label className="font-bold text-gray-700 block mb-1">Para (Destinatario):</label><select value={targetUserId} onChange={e=>setTargetUserId(e.target.value)} className="w-full p-2 text-xs rounded border border-gray-300 bg-white" required><option value="">Selecciona a un usuario de Inkorium...</option>{others.map(u=><option key={u.id} value={u.id}>{u.nombre} {u.apellidos} ({u.provincia})</option>)}</select></div><div><label className="font-bold text-gray-700 block mb-1">Asunto:</label><input value={subject} onChange={e=>setSubject(e.target.value)} placeholder="Escribe el asunto del mensaje..." className="w-full p-2 text-xs rounded border border-gray-300"/></div><div><label className="font-bold text-gray-700 block mb-1">Mensaje:</label><textarea rows={6} value={body} onChange={e=>setBody(e.target.value)} placeholder="Escribe el contenido de tu mensaje..." className="w-full p-2 text-xs rounded border border-gray-300 resize-none" required/></div><div className="flex justify-end gap-2 pt-2 border-t border-gray-200"><button type="button" onClick={()=>setMode('recibidos')} className="px-3.5 py-1.5 rounded bg-gray-100 text-gray-700 font-semibold">Cancelar</button><button type="submit" disabled={!targetUserId||!body.trim()||sending} className="px-5 py-1.5 rounded bg-[#3869A0] text-white font-bold disabled:opacity-50">{sending?'Enviando...':'Enviar mensaje'}</button></div></form></div>:<div className="space-y-3 text-xs"><h2 className="font-bold text-sm text-gray-900 pb-2 border-b border-gray-200">{mode==='enviados'?`Mensajes Enviados (${sent.length})`:`Mensajes Recibidos (${received.length})`}</h2>{(mode==='enviados'?sent:received).length===0?<div className="text-center py-16 text-gray-400 text-xs">{mode==='enviados'?'No has enviado ningún mensaje todavía.':'Tu bandeja de entrada está vacía.'}</div>:<div className="divide-y divide-gray-100">{(mode==='enviados'?sent:received).map(m=><div key={m.id} onClick={()=>void open(m)} className={`py-2.5 px-2 rounded cursor-pointer flex items-center justify-between gap-3 ${!m.leido&&mode==='recibidos'?'bg-blue-50/80':'hover:bg-gray-50'}`}><div className="flex items-center gap-3 overflow-hidden flex-1"><img src={mode==='enviados'?currentUser.avatar:m.emisorAvatar} alt="" className="w-8 h-8 rounded object-cover border border-gray-300"/><div className="overflow-hidden">{mode==='recibidos'?<div className="flex items-center gap-2"><span className="font-bold text-[#3869A0] text-xs truncate">{m.emisorNombre}</span>{!m.leido&&<span className="bg-[#3869A0] text-white text-[9px] font-bold px-1.5 rounded-full">Nuevo</span>}</div>:<p className="font-bold text-gray-900 text-xs truncate">Para: {m.receptorNombre}</p>}<p className="text-gray-900 text-xs font-semibold truncate">{m.asunto}</p><p className="text-[11px] text-gray-500 truncate">{m.mensaje}</p></div></div><span className="text-[10px] text-gray-400">{m.fecha}</span></div>)}</div>}</div>}</div></div></div></div>};
