import React, { useEffect, useState } from 'react';
import { supabase } from '../../lib/supabase';
import { usePlayerStore } from '../../lib/store';
import { Search, Heart, Share2, Plus, Music, Play, ListPlus, Loader2, UploadCloud, X, Users } from 'lucide-react';
import { createR2UploadTicket, uploadToPresignedUrl, getR2SignedUrl } from '../../lib/r2';

const blue = '#0750A7';
const border = '#dfe6ee';
const text = '#1f2e40';
const muted = '#718096';

type MusicTrack = { id?: string; title: string; artist?: string | null; source_type?: 'local' | 'youtube'; youtube_id?: string | null; audio_url?: string | null; cover_url?: string | null };
type YouTubeResult = { id: { videoId: string }; snippet: { title: string; channelTitle: string; thumbnails?: { high?: { url: string }; medium?: { url: string }; default?: { url: string } } } };

export function MusicView({ session }: { session: any; navigate: any }) {
  const [activeTab, setActiveTab] = useState<'descubrir' | 'buscar' | 'mi-musica' | 'playlists' | 'subir'>('descubrir');
  return <section className="content-view music-view" style={{ background: '#f3f6fa', minHeight: '100%', padding: '18px 20px' }}>
    <style>{`.feed-layout:has(.music-view){grid-template-columns:minmax(190px,280px) minmax(0,1fr)!important}.feed-layout:has(.music-view)>.right-column,.feed-layout:has(.music-view) .right-column,body:has(.music-view) .right-column{display:none!important}.music-shell{display:grid;grid-template-columns:minmax(0,1fr) 300px;gap:18px;max-width:1220px;margin:0 auto}.music-panel{background:#fff;border:1px solid ${border};border-radius:6px;box-shadow:0 1px 2px rgba(23,55,90,.03)}.music-tabs{display:flex;gap:26px;border-bottom:1px solid #e5eaf0;overflow:auto}.music-tab{display:flex;align-items:center;gap:7px;padding:13px 4px 12px;border:0;border-bottom:2px solid transparent;background:transparent;color:#51657b;font-weight:600;white-space:nowrap;cursor:pointer}.music-tab.active{color:${blue};border-bottom-color:${blue}}.music-search-row{display:flex;gap:10px}.music-search-input{flex:1;min-width:0;height:44px;padding:0 14px 0 40px;border:1px solid #d3dce6;border-radius:5px;outline:0;color:${text};background:#fff}.music-search-input:focus{border-color:${blue};box-shadow:0 0 0 2px rgba(7,80,167,.10)}.music-result{display:flex;align-items:center;gap:14px;padding:11px 0;border-bottom:1px solid #edf1f5}.music-result:last-child{border-bottom:0}.music-cover{width:72px;height:72px;flex:0 0 72px;border-radius:5px;object-fit:cover;background:#edf2f7}.music-action{display:grid;place-items:center;width:36px;height:36px;padding:0;border:1px solid #cbd7e4;border-radius:5px;color:#536b84;background:#fff;cursor:pointer}.music-action:hover{border-color:${blue};color:${blue};background:#f5f9ff}.music-action.primary{color:#fff;border-color:${blue};background:${blue}}.music-action.primary:hover{background:#06458f;color:#fff}.music-card{padding:16px}.music-side-card{padding:17px}.music-side-item{display:flex;gap:10px;padding:10px 0;border-bottom:1px solid #edf1f5}.music-side-item:last-child{border-bottom:0}.music-avatar{width:34px;height:34px;flex:0 0 34px;border-radius:50%;object-fit:cover;background:#e7eef6}.music-modal-backdrop{position:fixed;inset:0;z-index:2000;display:grid;place-items:center;padding:20px;background:rgba(20,34,52,.35)}.music-modal{width:min(520px,100%);max-height:min(680px,90vh);overflow:auto;background:#fff;border:1px solid ${border};border-radius:8px;box-shadow:0 20px 60px rgba(20,34,52,.25)}@media(max-width:900px){.music-shell{grid-template-columns:1fr}.music-sidebar{order:2}}@media(max-width:700px){.feed-layout:has(.music-view){grid-template-columns:1fr!important}.music-view{padding:10px}.music-result{align-items:flex-start}.music-result-actions{flex-wrap:wrap}.music-cover{width:64px;height:64px;flex-basis:64px}}`}</style>
    <div className="music-shell"><div className="music-main"><div className="music-panel" style={{ padding: '22px' }}><header style={{ marginBottom: 8 }}><h1 style={{ margin: 0, color: text, fontSize: 30 }}>Música</h1><p style={{ margin: '5px 0 0', color: '#58708a', fontSize: 16 }}>Escucha, comparte y descubre música con tus amigos.</p></header><nav className="music-tabs"><Tab active={activeTab==='descubrir'} onClick={()=>setActiveTab('descubrir')} icon={<Heart size={17}/>} label="Descubrir"/><Tab active={activeTab==='buscar'} onClick={()=>setActiveTab('buscar')} icon={<Search size={17}/>} label="Buscar"/><Tab active={activeTab==='mi-musica'} onClick={()=>setActiveTab('mi-musica')} icon={<Music size={17}/>} label="Mi música"/><Tab active={activeTab==='playlists'} onClick={()=>setActiveTab('playlists')} icon={<ListPlus size={17}/>} label="Playlists"/><Tab active={activeTab==='subir'} onClick={()=>setActiveTab('subir')} icon={<UploadCloud size={17}/>} label="Subir canción"/></nav></div><div style={{ marginTop: 14 }}>{activeTab==='buscar'&&<MusicSearch session={session}/>} {activeTab==='descubrir'&&<MusicDiscover onSearch={()=>setActiveTab('buscar')}/>} {activeTab==='mi-musica'&&<MyMusic session={session}/>} {activeTab==='playlists'&&<PlaylistsTab session={session}/>} {activeTab==='subir'&&<MusicUpload session={session} onDone={()=>setActiveTab('mi-musica')}/>}</div></div><aside className="music-sidebar" style={{display:'flex',flexDirection:'column',gap:14}}><MusicActivitySidebar/><FriendsPlaylistsSidebar session={session}/></aside></div>
  </section>;
}
function Tab({active,onClick,icon,label}:{active:boolean;onClick:()=>void;icon:React.ReactNode;label:string}){return <button className={`music-tab${active?' active':''}`} onClick={onClick}>{icon}{label}</button>}
function Card({children,style}:{children:React.ReactNode;style?:React.CSSProperties}){return <div className="music-panel music-card" style={style}>{children}</div>}
function MusicActivitySidebar(){const[activities,setActivities]=useState<any[]>([]);useEffect(()=>{(async()=>{const{data}=await supabase.from('music_activity').select('*, profiles:user_id(username, full_name, avatar_url), music_tracks:track_id(*)').order('created_at',{ascending:false}).limit(12);setActivities(data||[])})()},[]);return <div className="music-panel music-side-card"><div style={{display:'flex',alignItems:'center',gap:8,marginBottom:6}}><Music size={18} color={blue}/><strong style={{color:text,fontSize:17}}>Actividad musical</strong></div>{activities.length===0?<p style={{color:muted,fontStyle:'italic'}}>No hay actividad reciente.</p>:activities.map(a=><div className="music-side-item" key={a.id}><img className="music-avatar" src={a.profiles?.avatar_url||'/default-avatar.png'} alt=""/><div style={{minWidth:0,fontSize:13}}><strong style={{color:text}}>{a.profiles?.full_name||a.profiles?.username||'Usuario'}</strong><span style={{color:muted}}> {a.action} </span><strong style={{color:text}}>{a.music_tracks?.title||'una canción'}</strong></div></div>)}</div>}
function FriendsPlaylistsSidebar({session}:{session:any}){const[playlists,setPlaylists]=useState<any[]>([]);useEffect(()=>{(async()=>{const{data}=await supabase.from('music_playlists').select('*, profiles:user_id(username, full_name, avatar_url)').eq('is_public',true).neq('user_id',session.user.id).order('created_at',{ascending:false}).limit(5);setPlaylists(data||[])})()},[session.user.id]);return <div className="music-panel music-side-card"><div style={{display:'flex',alignItems:'center',gap:8,marginBottom:6}}><Users size={18} color={blue}/><strong style={{color:text,fontSize:17}}>Playlists de amigos</strong></div>{playlists.map(p=><div className="music-side-item" key={p.id}><div style={{width:44,height:44,flex:'0 0 44px',borderRadius:5,overflow:'hidden',background:'#edf2f7',display:'grid',placeItems:'center'}}>{p.cover_url?<img src={p.cover_url} alt="" style={{width:'100%',height:'100%',objectFit:'cover'}}/>:<ListPlus size={18} color={muted}/>}</div><div><strong style={{display:'block',color:text,fontSize:13}}>{p.name}</strong><span style={{color:muted,fontSize:11}}>Por {p.profiles?.full_name||p.profiles?.username||'Usuario'}</span></div></div>)}</div>}
function MusicSearch({session}:{session:any}){const[query,setQuery]=useState('');const[results,setResults]=useState<YouTubeResult[]>([]);const[searching,setSearching]=useState(false);const[error,setError]=useState('');const[notice,setNotice]=useState('');async function handleSearch(e:React.FormEvent){e.preventDefault();const value=query.trim();if(!value)return;setSearching(true);setError('');try{const key=import.meta.env.VITE_YOUTUBE_API_KEY;if(!key)throw new Error('Falta VITE_YOUTUBE_API_KEY');const response=await fetch(`https://www.googleapis.com/youtube/v3/search?part=snippet&q=${encodeURIComponent(value)}&type=video&maxResults=15&key=${key}`);const data=await response.json();if(!response.ok)throw new Error(data?.error?.message||'No se pudo buscar música.');setResults((data.items||[]).filter((x:YouTubeResult)=>x?.id?.videoId))}catch(err:any){setError(err?.message||'No se pudo completar la búsqueda.')}finally{setSearching(false)}}async function save(track:MusicTrack){const{data:existing}=await supabase.from('music_tracks').select('id').eq('source_type','youtube').eq('youtube_id',track.youtube_id).maybeSingle();if(existing?.id){setNotice('Canción guardada.');return}const{error}=await supabase.from('music_tracks').insert({user_id:session.user.id,title:track.title,artist:track.artist,source_type:'youtube',youtube_id:track.youtube_id,cover_url:track.cover_url});setNotice(error?.message||'Canción guardada en tu música.')}return <Card><h2 style={{margin:'0 0 15px',color:text,fontSize:22}}>Buscar música</h2><form onSubmit={handleSearch} className="music-search-row"><div style={{position:'relative',flex:1}}><Search size={18} color="#8192a5" style={{position:'absolute',left:13,top:13}}/><input className="music-search-input" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar canciones, artistas, vídeos..."/></div><button type="submit" disabled={searching} style={{minWidth:96,border:0,borderRadius:5,background:blue,color:'#fff',fontWeight:700}}>{searching?<Loader2 size={18}/>:<span>Buscar</span>}</button></form>{error&&<p style={{color:'#a52828'}}>{error}</p>}{notice&&<p style={{color:'#24613b'}}>{notice}</p>}<div>{results.map(item=>{const track:MusicTrack={source_type:'youtube',youtube_id:item.id.videoId,title:item.snippet.title,artist:item.snippet.channelTitle,cover_url:item.snippet.thumbnails?.high?.url||item.snippet.thumbnails?.medium?.url||item.snippet.thumbnails?.default?.url};return <div className="music-result" key={track.youtube_id}><img className="music-cover" src={track.cover_url||'/default-avatar.png'} alt=""/><div style={{flex:1,minWidth:0}}><strong style={{color:text}}>{track.title}</strong><div style={{color:muted,fontSize:13}}>{track.artist}</div></div><button className="music-action primary" onClick={()=>usePlayerStore.getState().playSong({source_type:'youtube',video_id:track.youtube_id,title:track.title,artist:track.artist||'',thumbnail:track.cover_url||undefined})}><Play size={16}/></button><button className="music-action" onClick={()=>void save(track)}><Plus size={16}/></button></div>})}</div></Card>}
function MusicDiscover({onSearch}:{onSearch:()=>void}){return <div style={{display:'grid',gap:14}}><Card><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><div><h2 style={{margin:0,color:text,fontSize:21}}>Descubrir</h2><p style={{color:muted}}>Música compartida recientemente por la comunidad.</p></div><button onClick={onSearch} className="music-action"><Search size={16}/></button></div></Card></div>}
function MyMusic({session}:{session:any}){const[tracks,setTracks]=useState<any[]>([]);const[loading,setLoading]=useState(true);const player=usePlayerStore();useEffect(()=>{(async()=>{const{data}=await supabase.from('music_tracks').select('*').eq('user_id',session.user.id).order('created_at',{ascending:false});setTracks(data||[]);setLoading(false)})()},[session.user.id]);if(loading)return <Card><Loader2 className="spin" size={24}/></Card>;return <Card><h2 style={{margin:'0 0 16px',color:text,fontSize:22}}>Mi música</h2>{tracks.length===0?<p style={{color:muted}}>Todavía no has subido canciones.</p>:tracks.map(track=>{const cover=track.cover_url||(track.source_type==='youtube'&&track.youtube_id?`https://i.ytimg.com/vi/${track.youtube_id}/hqdefault.jpg`:null);const play=async()=>{if(track.source_type==='youtube'){player.playSong({source_type:'youtube',video_id:track.youtube_id||undefined,title:track.title,artist:track.artist||'',thumbnail:cover||undefined,id:track.id});return}if(!track.audio_url)return;try{const url=track.audio_url.startsWith('r2://')?await getR2SignedUrl(track.audio_url.slice(5)):track.audio_url;player.playSong({source_type:'local',audio_url:url,title:track.title,artist:track.artist||'',thumbnail:cover||undefined,id:track.id})}catch(err){console.error('[R2_PLAYBACK]',err)}};return <div className="music-result" key={track.id}><img className="music-cover" src={cover||'/default-avatar.png'} alt=""/><div style={{flex:1}}><strong style={{color:text}}>{track.title}</strong><div style={{color:muted,fontSize:13}}>{track.artist}</div></div><button className="music-action primary" onClick={()=>void play()}><Play size={16}/></button></div>})}</Card>}
function PlaylistsTab({session}:{session:any}){const[playlists,setPlaylists]=useState<any[]>([]);const[name,setName]=useState('');const[creating,setCreating]=useState(false);async function load(){const{data}=await supabase.from('music_playlists').select('*').eq('user_id',session.user.id).order('created_at',{ascending:false});setPlaylists(data||[])}useEffect(()=>{void load()},[session.user.id]);async function create(){if(!name.trim())return;setCreating(true);const{error}=await supabase.from('music_playlists').insert({user_id:session.user.id,name:name.trim(),is_public:true});if(error)console.error(error);else{setName('');await load()}setCreating(false)}return <Card><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginBottom:16}}><h2 style={{margin:0,color:text,fontSize:22}}>Mis playlists</h2><div style={{display:'flex',gap:7}}><input value={name} onChange={e=>setName(e.target.value)} placeholder="Nueva playlist"/><button onClick={create} disabled={creating||!name.trim()} className="music-action primary"><Plus size={17}/></button></div></div>{playlists.map(p=><div key={p.id} style={{padding:10,borderBottom:`1px solid ${border}`}}><strong>{p.name}</strong></div>)}</Card>}
function MusicUpload({session,onDone}:{session:any;onDone:()=>void}){const[file,setFile]=useState<File|null>(null);const[title,setTitle]=useState('');const[artist,setArtist]=useState('');const[uploading,setUploading]=useState(false);const[error,setError]=useState('');async function upload(e:React.FormEvent){e.preventDefault();if(!file||!title.trim()||!artist.trim())return;if(!file.type.startsWith('audio/'))return setError('Solo se permiten archivos de audio.');if(file.size>250*1024*1024)return setError('El archivo es demasiado grande. Máximo 250MB.');setUploading(true);setError('');try{const ticket=await createR2UploadTicket({folder:'music',file});await uploadToPresignedUrl(ticket.uploadUrl,file,file.type);const audioUrl=ticket.url||`r2://${ticket.key}`;const{error:dbError}=await supabase.from('music_tracks').insert({user_id:session.user.id,title:title.trim(),artist:artist.trim(),source_type:'local',audio_url:audioUrl});if(dbError)throw dbError;setFile(null);setTitle('');setArtist('');onDone()}catch(err:any){setError(err?.message||'No se pudo subir la canción.')}finally{setUploading(false)}}return <Card><h2 style={{margin:'0 0 16px',color:text,fontSize:22}}>Subir canción</h2>{error&&<p style={{color:'#b52a2a'}}>{error}</p>}<form onSubmit={upload} style={{display:'grid',gap:13,maxWidth:560}}><label>Archivo de audio<input type="file" accept="audio/*" onChange={e=>setFile(e.target.files?.[0]||null)}/></label><label>Título<input value={title} onChange={e=>setTitle(e.target.value)} /></label><label>Artista<input value={artist} onChange={e=>setArtist(e.target.value)} /></label><button type="submit" disabled={uploading||!file||!title.trim()||!artist.trim()}>{uploading?<Loader2 className="spin" size={18}/>:<><UploadCloud size={17}/>Subir canción</>}</button></form></Card>}
