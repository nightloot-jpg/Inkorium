const EVENT_LABEL = 'Eventos';
const EVENT_PAGE = 'eventos';
function mountEventsEntry(){const menus=Array.from(document.querySelectorAll<HTMLElement>('.side-menu'));if(!menus.length)return;menus.forEach(menu=>{const existing=Array.from(menu.querySelectorAll<HTMLElement>('button,a')).find(el=>el.textContent?.trim().toLowerCase()===EVENT_LABEL.toLowerCase());if(existing)return;const button=document.createElement('button');button.type='button';button.dataset.page=EVENT_PAGE;button.setAttribute('aria-label',EVENT_LABEL);button.innerHTML='<span aria-hidden="true">□</span>Eventos';button.addEventListener('click',()=>{sessionStorage.setItem('inkorium-page',EVENT_PAGE);sessionStorage.removeItem('inkorium-route-params');window.dispatchEvent(new CustomEvent('inkorium-route-change',{detail:{page:EVENT_PAGE}}));});menu.appendChild(button);});}
let scheduled=false;function scheduleMount(){if(scheduled)return;scheduled=true;window.setTimeout(()=>{scheduled=false;mountEventsEntry();},0);}scheduleMount();const observer=new MutationObserver(scheduleMount);observer.observe(document.body,{childList:true,subtree:true});
