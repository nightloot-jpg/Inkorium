# Feed

The Feed component was extracted from `src/main.tsx` as part of the route architecture cleanup.

This step only changes module ownership. Legacy route state remains inside `Feed` until the next cleanup step.