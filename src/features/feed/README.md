# Feed

Feed is the global shell and home feed. Internal pages are owned by RouteContentBridge.

Feed keeps only the minimal route signal needed to highlight the global navigation and render the home feed. It does not own page history or render bridged pages.
