// This file intentionally contains no runtime code. It documents the auth handoff
// performed by boot.tsx so the login page can transition into the React app without
// requiring a browser refresh.
export {};
