# r2-media

Signs direct browser uploads to Cloudflare R2 for user images. Configure the existing R2 secrets used by `r2-video` plus `R2_PUBLIC_BASE_URL` pointing to the public/custom-domain base URL for the bucket, for example `https://media.inkorium.es`.
