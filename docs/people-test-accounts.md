# Test accounts in Personas

The seeded test-account IDs are filtered from the Personas directory in `src/PeopleView.tsx` so deleted or restored test fixtures cannot reappear in the UI.

Production users are not affected.
